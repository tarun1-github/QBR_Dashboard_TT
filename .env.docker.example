# Runtime environment template for the QBR Docker container.
# Copy to .env.docker on the deployment host and fill in real values.
DATABASE_SERVER=HQ-AD-DNS-CLONE.cciecollab.cisco.com\SQLEXPRESS
DATABASE_NAME=CPDB
DATABASE_USER=
DB_PASSWORD=
DB_DRIVER=ODBC Driver 18 for SQL Server
QBR_COOKIE_SECRET=
APP_TITLE=QBR Ticket Alert Dashboard
